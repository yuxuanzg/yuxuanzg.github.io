# 修改指南 —— 你的网页地址 & 如何改内容

## 一、网页地址

| 阶段 | 地址 |
| --- | --- |
| 本地预览（现在就能看） | http://127.0.0.1:8000 |
| 上线后（正式地址） | **https://yuxuanzg.github.io** |

> 正式地址需要完成 3 步才能生效：
> ① GitHub 创建仓库 `yuxuanzg.github.io` → ② 双击 `deploy.bat` 推送 → ③ Settings → Pages 启用 main 分支。

---

## 二、修改流程（记住这个循环）

```
1. 编辑文件  →  2. 浏览器刷新看效果  →  3. git 推送上线
```

- **编辑**：用记事本 / VS Code / 任何文本编辑器打开 `webpage` 文件夹里的文件
- **预览**：改完保存后，刷新浏览器里的 http://127.0.0.1:8000（无需重启服务器）
- **上线**：双击 `deploy.bat`（或在项目目录执行 git 提交推送）

---

## 三、想改什么，就改哪个文件

| 你想改的内容 | 打开的文件 | 在哪里找 |
| --- | --- | --- |
| 姓名 / 头衔 / 研究方向标签 | `index.html` | 顶部「Hero」区（搜索 `hero-name` / `hero-sub` / `hero-tag`） |
| 个人简介 | `index.html` | 「关于我」区块（搜索 `about-text`） |
| 基本信息卡片（单位/邮箱等） | `index.html` | 「基本信息」列表（搜索 `info-list`） |
| 头像照片 | `assets/` 文件夹 | 放入你的照片 → 修改 `index.html` 中头像的 `src` |
| 研究方向卡片 | `index.html` | 搜索 `topic-card` |
| 教育经历 | `index.html` | 搜索 `timeline` |
| 项目经历 | `index.html` 和 `projects.html` | 搜索 `project-card` |
| 联系方式 | `index.html` | 搜索 `contact-card` |
| 博客列表 | `blog/index.html` | 搜索 `post-item` |
| 文章内容 | `blog/posts/` 下的文件 | 每篇一个 HTML 文件 |
| 网站标题 / 图标 | 每个 html 的 `<head>` | `<title>` 和 `<link rel="icon">` |
| 配色 / 风格 | `css/style.css` | 顶部 `:root` 里的颜色变量 |

---

## 四、常见修改示例

### 示例 1：修改个人简介
1. 打开 `index.html`，找到「关于我」区块的 `<p class="about-text">`
2. 把文字替换成你自己的介绍
3. 保存 → 刷新预览 → 双击 `deploy.bat` 上线

### 示例 2：更换头像照片
1. 把你的照片命名为 `avatar.jpg`（或 png），放入 `assets/` 文件夹
2. 打开 `index.html`，找到：
   `<img class="avatar" src="assets/avatar.svg" alt="诸葛宇轩 的头像" />`
3. 把 `assets/avatar.svg` 改成 `assets/avatar.jpg`（文件名要完全一致，注意大小写）

### 示例 3：新增一条教育经历
在 `index.html` 的教育经历时间线里，复制一段 `timeline-item` 结构，修改学校、时间和描述即可。

### 示例 4：发布一篇新博客
1. 复制 `blog/posts/hello-world.html`，改名为如 `blog/posts/我的第一篇笔记.html`（建议用英文名，如 `my-first-note.html`）
2. 编辑标题和正文（正文用 `<h2>` 分节、`<p>` 写段落）
3. 打开 `blog/index.html`，复制一条 `post-item` 链接，把 `href` 和标题改成你的新文章

---

## 五、上线后如何更新

每次修改完内容，在项目根目录执行：

```bash
"C:\Program Files\Git\bin\git.exe" add -A
"C:\Program Files\Git\bin\git.exe" commit -m "更新内容"
"C:\Program Files\Git\bin\git.exe" push
```

或者更简单：直接双击 `deploy.bat`（它会重新推送全部改动）。
GitHub 会在 1–2 分钟内自动更新线上页面。
